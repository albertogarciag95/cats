language: node_js
node_js:
  - '12'
addons:
  chrome: stable
branches:
  - develop
  - /^feature/.*$/
  - master
cache:
  directories:
     - ./node_modules
notifications:
  email:
    recipients:
      - alberto95.rm@gmail.com
before_script :
 - curl -o- -L https://yarnpkg.com/install.sh | bash -s
 - export PATH=$HOME/.yarn/bin:$PATH
 - yarn install
script:
  - yarn test
# test
# deploy:
#   provider: heroku
#   api_key:
#     secure: $HEROKU
#   on: